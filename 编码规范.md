# AgentManage 项目编码规范

## 1. 概述

本文档定义了 OpenClaw AI Agent项目管理系统的编码规范，旨在确保代码质量、可读性和可维护性。所有开发人员必须遵循本规范进行编码。

## 2. 通用规范

### 2.1 命名规范

#### 变量命名
- **使用有意义的名称**：变量名必须能够描述其用途
- **驼峰命名法**：使用 camelCase 命名变量
- **常量**：使用全大写字母，下划线分隔
- **布尔变量**：使用 `is`、`has`、`can`、`should` 前缀

```typescript
// ✅ 正确示例
let userName: string = '张三';
let isActive: boolean = true;
let MAX_RETRY_COUNT: number = 3;
const apiBaseUrl: string = 'https://api.example.com';

// ❌ 错误示例
let n: string = '张三';          // 名称无意义
let flag: boolean = true;       // 名称不明确
let max: number = 3;            // 不够明确
```

#### 函数命名
- **使用动词或动词短语**：描述函数执行的操作
- **驼峰命名法**：首字母小写，后续每个单词首字母大写
- **避免过长**：保持简洁，不超过 30 个字符

```typescript
// ✅ 正确示例
function getUserList(): User[] { ... }
function createProject(name: string): Project { ... }
function updateTaskStatus(taskId: string, status: string): void { ... }

// ❌ 错误示例
function getList(): User[] { ... }           // 名称不够明确
function doSomething(): void { ... }         // 名称无意义
function createProjectNameIfNotExists(name: string): Project { ... }  // 名称过长
```

#### 类命名
- **使用 PascalCase**：每个单词首字母大写
- **使用名词**：描述类的用途
- **避免缩写**：除非是广泛认可的缩写

```typescript
// ✅ 正确示例
class ProjectManager { ... }
class TaskService { ... }
class UserController { ... }

// ❌ 错误示例
class ProjMgr { ... }           // 使用缩写
class manager { ... }           // 未使用 PascalCase
```

#### 文件命名
- **使用 kebab-case**：全部小写，单词之间用连字符分隔
- **组件文件**：使用组件名作为文件名
- **工具文件**：使用 `utils` 后缀

```typescript
// ✅ 正确示例
project-list.tsx
user-service.ts
api-client.ts
task-utils.ts

// ❌ 错误示例
ProjectList.tsx      // 使用 PascalCase
projectList.tsx      // 使用 camelCase
project.list.tsx     // 使用点号
```

### 2.2 代码格式

#### 缩进和空格
- **使用 2 空格**：不要使用 Tab
- **行尾不保留空格**：删除行尾多余空格
- **运算符前后空格**：保持一致

```typescript
// ✅ 正确示例
const sum = a + b;
const isValid = a === b;
function foo(a: string, b: number) { ... }

// ❌ 错误示例
const sum = a+b;          // 缺少空格
const isValid = a===b;    // 缺少空格
function foo(a:string,b:number){ ... }  // 缺少空格
```

#### 花括号
- **使用 K&R 风格**：左花括号不换行
- **始终使用花括号**：即使只有一条语句

```typescript
// ✅ 正确示例
if (isActive) {
    doSomething();
}

if (isValid) {
    return true;
} else {
    return false;
}

// ❌ 错误示例
if (isActive)
    doSomething();

if (isValid) return true;
else return false;
```

#### 引号
- **使用单引号**：字符串使用单引号
- **JSX 属性使用双引号**：保持与 HTML 一致

```typescript
// ✅ 正确示例
const name: string = '张三';
const message: string = "Hello World";
<div className="container">...</div>

// ❌ 错误示例
const name: string = "张三";
const message: string = 'Hello World';
```

### 2.3 注释规范

#### 注释原则
- **注释率 ≥ 30%**：确保代码有足够的注释
- **解释为什么**：不要只描述是什么，要解释为什么
- **保持更新**：修改代码时同步更新注释

#### 注释类型

```typescript
// 单行注释：用于解释下一步操作
// 获取用户列表
const userList: User[] = await getUsers();

// 行内注释：用于解释复杂逻辑
const result = a + b; // 计算总和

/**
 * 多行注释：用于函数和类的文档
 * @param userId 用户ID
 * @returns 用户信息
 */
function getUserById(userId: string): User { ... }
```

#### JSDoc 注释
```typescript
/**
 * 创建项目
 * @param name 项目名称
 * @param description 项目描述
 * @param priority 优先级 (P0|P1|P2|P3)
 * @returns 创建成功的项目对象
 * @throws {Error} 项目创建失败时抛出
 */
function createProject(name: string, description: string, priority: string): Project {
    // 实现逻辑
}
```

## 3. TypeScript 规范

### 3.1 类型定义

#### 接口 vs 类型
- **使用接口定义对象结构**：更清晰，易于扩展
- **使用类型定义联合类型、交叉类型**

```typescript
// ✅ 正确示例
interface User {
    id: string;
    name: string;
    email: string;
}

type UserStatus = 'active' | 'inactive' | 'pending';
type UserWithRole = User & { role: string };

// ❌ 错误示例
type User = {
    id: string;
    name: string;
};
```

#### 类型推断
- **优先使用类型推断**：不要过度显式声明类型
- **复杂函数显式声明**：明确返回值类型

```typescript
// ✅ 正确示例
const count = 0;                    // 推断为 number
const name = '张三';                // 推断为 string
const isActive = true;              // 推断为 boolean
function getUserById(id: string) {  // 明确声明返回值类型
    return {} as User;
}

// ❌ 错误示例
const count: number = 0;            // 过度声明
const name: string = '张三';        // 过度声明
```

#### 可选属性和只读属性
- **使用可选属性表示非必需字段**
- **使用只读属性保护不变的数据**

```typescript
interface User {
    id: string;
    name: string;
    email?: string;       // 可选属性
    readonly createdAt: Date;  // 只读属性
}
```

### 3.2 函数规范

#### 函数参数
- **使用默认参数**：避免 undefined 检查
- **限制参数数量**：不超过 5 个参数
- **使用对象参数**：超过 3 个参数时使用对象

```typescript
// ✅ 正确示例
function createUser(name: string, email: string, role: string = 'user'): User {
    return { name, email, role };
}

function updateProject(config: {
    name?: string;
    description?: string;
    priority?: string;
}): void { ... }

// ❌ 错误示例
function createUser(name: string, email?: string, role?: string, status?: string): User {
    return { name, email: email || 'default', role: role || 'user' };
}
```

#### 箭头函数
- **简短函数使用箭头函数**
- **保持一致性**：整个项目统一风格

```typescript
// ✅ 正确示例
const add = (a: number, b: number): number => a + b;
const getUserList = () => userList.filter(u => u.isActive);

// 较长的函数使用普通函数
function processUserData(user: User): ProcessedUser {
    // 处理逻辑
    return {
        id: user.id,
        name: user.name.toUpperCase(),
        // ...
    };
}
```

### 3.3 模块规范

#### 导入顺序
1. 外部库导入（React, Ant Design 等）
2. 内部模块导入（项目模块）
3. 样式导入
4. 类型导入

```typescript
// ✅ 正确示例
import React, { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { Button, Table, Modal } from 'antd';
import { ProjectService } from '@/services/project';
import { UserService } from '@/services/user';
import styles from './index.module.scss';
import { User, Project } from '@/types';
```

#### 导出方式
- **使用命名导出**：便于 tree-shaking
- **避免默认导出**：保持一致性

```typescript
// ✅ 正确示例
export const ProjectService = { ... };
export function createProject(config: ProjectConfig): Project { ... }
export interface Project { ... }

// ❌ 错误示例
export default ProjectService;
```

## 4. React 规范

### 4.1 组件规范

#### 组件命名
- **使用 PascalCase**：组件名首字母大写
- **文件名与组件名一致**

```typescript
// ✅ 正确示例
// ProjectList.tsx
export const ProjectList: React.FC = () => { ... }

// ❌ 错误示例
// projectList.tsx
export const projectList = () => { ... }
```

#### 组件结构
```typescript
// ✅ 推荐组件结构
import React from 'react';
import { useState, useEffect } from 'react';
import { Button } from 'antd';
import styles from './ProjectList.module.scss';

interface ProjectListProps {
    projects: Project[];
    onSelect: (project: Project) => void;
}

export const ProjectList: React.FC<ProjectListProps> = ({ 
    projects, 
    onSelect 
}) => {
    // 状态
    const [loading, setLoading] = useState(false);
    
    // 副作用
    useEffect(() => {
        // 副作用逻辑
    }, []);
    
    // 事件处理
    const handleClick = (project: Project) => {
        onSelect(project);
    };
    
    // 渲染
    return (
        <div className={styles.container}>
            {projects.map(project => (
                <div key={project.id} onClick={() => handleClick(project)}>
                    {project.name}
                </div>
            ))}
        </div>
    );
};
```

#### Props 命名
- **使用 on 前缀**：表示事件处理函数
- **使用 handle 前缀**：表示内部事件处理函数

```typescript
// ✅ 正确示例
interface ButtonProps {
    onClick: () => void;           // 外部传入的事件处理
    onChange?: (value: string) => void;
}

const Button: React.FC<ButtonProps> = ({ onClick, onChange }) => {
    const handleClick = () => {    // 内部处理函数
        onClick();
    };
    
    return <button onClick={handleClick}>Click</button>;
};
```

### 4.2 Hooks 规范

#### Hooks 命名
- **以 use 开头**：自定义 Hook 必须以 use 开头
- **保持简洁**：每个 Hook 只做一件事

```typescript
// ✅ 正确示例
const useUserList = (params: UserQueryParams) => {
    const [users, setUsers] = useState<User[]>([]);
    const [loading, setLoading] = useState(false);
    
    useEffect(() => {
        loadUsers();
    }, [params]);
    
    return { users, loading };
};

// ❌ 错误示例
const getUserList = (params) => {  // 缺少 use 前缀
    // ...
}
```

#### 依赖数组
- **完整声明依赖**：不要遗漏依赖
- **使用 ESLint 规则**：确保依赖正确

```typescript
// ✅ 正确示例
useEffect(() => {
    const subscription = api.subscribe(id, handleChange);
    return () => subscription.unsubscribe();
}, [id, handleChange]);

// ❌ 错误示例
useEffect(() => {
    api.subscribe(id, handleChange);
}, []);  // 缺少依赖
```

### 4.3 状态管理

#### 状态放置原则
- **组件本地状态**：使用 useState
- **跨组件共享**：使用 Context 或状态管理库
- **服务器状态**：使用 React Query 或 SWR

```typescript
// ✅ 正确示例
// 本地状态
const [count, setCount] = useState(0);

// 共享状态（使用 Context）
const UserContext = createContext<UserContextType>(null);

// 服务器状态（使用 React Query）
const { data, isLoading } = useQuery(['user', userId], () => 
    fetchUser(userId)
);
```

## 5. 样式规范

### 5.1 CSS Modules

#### 命名规范
- **使用 kebab-case**：类名使用连字符
- **使用 BEM 命名**：块-元素-修饰符

```css
/* ✅ 正确示例 */
.container { }
.container__header { }
.container__header--active { }
.button { }
.button--primary { }
.button--primary--disabled { }

/* ❌ 错误示例 */
.containerHeader { }           // 使用驼峰
.container_header { }          // 使用下划线
```

#### 模块化
- **每个组件独立样式文件**
- **避免全局样式**

```typescript
// ✅ 正确示例
import styles from './ProjectList.module.scss';

export const ProjectList: React.FC = () => {
    return (
        <div className={styles.container}>
            <div className={styles.header}></div>
        </div>
    );
};
```

### 5.2 设计系统

#### 使用设计变量
- **使用 CSS 变量**：保持一致性
- **使用 Ant Design 主题**：保持风格统一

```css
/* ✅ 正确示例 */
:root {
    --primary-color: #1890ff;
    --border-radius: 8px;
}

.container {
    background-color: var(--primary-color);
    border-radius: var(--border-radius);
}
```

## 6. API 规范

### 6.1 请求处理

#### 错误处理
- **统一错误处理**：使用拦截器
- **用户友好提示**：显示清晰的错误信息

```typescript
// ✅ 正确示例
try {
    const response = await api.get('/projects');
    if (response.code === 0) {
        return response.data;
    }
    throw new Error(response.msg);
} catch (error) {
    message.error('获取项目列表失败，请稍后重试');
    throw error;
}
```

#### 请求参数
- **类型安全**：使用 TypeScript 类型
- **参数校验**：使用 Zod 或 Yup

```typescript
// ✅ 正确示例
interface CreateProjectParams {
    name: string;
    description?: string;
    priority: 'P0' | 'P1' | 'P2' | 'P3';
    startDate: string;
    endDate: string;
}

const createProject = async (params: CreateProjectParams) => {
    // 参数校验
    if (!params.name) {
        throw new Error('项目名称不能为空');
    }
    // 发送请求
};
```

### 6.2 响应格式

```typescript
// 统一响应格式
interface ApiResponse<T> {
    code: number;        // 0 表示成功
    msg: string;         // 响应消息
    data: T;             // 响应数据
    trace_id?: string;   // 请求追踪ID
}
```

## 7. 测试规范

### 7.1 单元测试

#### 测试文件命名
- **与源文件同名**：使用 `.test.ts` 或 `.spec.ts` 后缀
- **放在源文件旁边**：便于查找

```typescript
// ✅ 正确示例
project.service.ts
project.service.test.ts

// ❌ 错误示例
project.service.ts
__tests__/project.test.ts
```

#### 测试用例结构

```typescript
// ✅ 正确示例
describe('ProjectService', () => {
    describe('createProject', () => {
        it('应该成功创建项目', async () => {
            const project = await ProjectService.createProject({
                name: '测试项目',
                priority: 'P1',
            });
            expect(project.id).toBeDefined();
        });
        
        it('项目名称为空时应该抛出错误', async () => {
            await expect(
                ProjectService.createProject({ name: '' })
            ).rejects.toThrow('项目名称不能为空');
        });
    });
});
```

### 7.2 测试覆盖率

- **目标覆盖率 ≥ 60%**
- **重点测试核心业务逻辑**
- **测试边界条件和异常情况**

## 8. Git 提交规范

### 8.1 提交信息格式

```
<类型>(<范围>): <简短描述>

# 详细描述（可选）

# 关闭的 Issue（可选）
Closes #123
```

### 8.2 类型说明

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | Bug 修复 |
| docs | 文档更新 |
| style | 代码格式 |
| refactor | 重构 |
| test | 测试相关 |
| chore | 构建/工具相关 |

### 8.3 提交示例

```bash
# 功能开发
git commit -m "feat(项目): 添加项目创建功能"

# Bug 修复
git commit -m "fix(任务): 修复任务状态更新失败问题"

# 文档更新
git commit -m "docs(接口): 更新 API 接口文档"

# 代码重构
git commit -m "refactor(用户): 重构用户权限验证逻辑"

# 测试
git commit -m "test(项目): 添加项目模块单元测试"
```

## 9. 代码审查要点

### 9.1 必查项

- [ ] 代码风格符合规范
- [ ] 命名清晰有意义
- [ ] 注释充分（≥30%）
- [ ] 类型定义完整
- [ ] 错误处理完善
- [ ] 单元测试通过
- [ ] 无安全漏洞

### 9.2 建议项

- [ ] 代码可以复用
- [ ] 性能良好
- [ ] 可访问性良好
- [ ] 文档已更新

## 10. 违规处理

### 10.1 代码质量门禁

- 代码注释率 < 30% → 警告
- 单元测试覆盖率 < 60% → 禁止合并
- ESLint 检查失败 → 禁止合并
- TypeScript 编译错误 → 禁止合并

### 10.2 处罚措施

- 首次违反：口头警告
- 二次违反：代码审查不通过
- 三次违反：停止开发权限一天

---

**文档版本:** v1.0
**创建日期:** 2026-03-13
**最后更新:** 2026-03-13
**创建人:** 团队共同制定
