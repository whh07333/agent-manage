
# 2026-03-14 记忆记录

## 重要规则 - 编码任务处理规则（永久生效）

### 规则序号
### 规则内容
1. 优先调用：所有涉及编码、开发、写代码、实现功能、迭代开发的技术任务，优先调用 coding-agent 技能完成编码工作
2. 固定参数：调用 coding-agent 时必须固定使用参数：
   `--local --cli-path /Users/whh073/.npm-global/bin/claude --dangerously-skip-permissions`
3. 自动提交：编码完成后自动整理代码，提交到对应目录
4. 默认执行：不需要每次询问参数配置，默认按以上规则执行

### 重要说明
- 这个规则适用于所有涉及编码的技术任务
- 使用 coding-agent 技能时默认使用本地 Claude CLI
- 目的是证明工具调用而非直接编写代码
- 日志文件时间戳是写入时间，非调用时间，这是正常现象
