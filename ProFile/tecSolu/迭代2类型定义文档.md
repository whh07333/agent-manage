# 迭代2类型定义文档

**文档信息**
- 项目名称：OpenClaw AI Agent专属项目管理系统
- 迭代版本：Iteration 2 (v1.1)
- 文档版本：v1.0
- 创建时间：2026-03-26
- 负责人：Mike（后端开发工程师）
- 说明：本文档包含迭代2所有后端类型定义，统一使用驼峰命名

---

## 一、类型定义规范

### 1.1 命名规范

| 层级 | 命名风格 | 说明 |
|------|---------|------|
| **数据库字段** | 下划线命名 | `assignee_id`, `project_id` |
| **Sequelize模型** | 驼峰命名 | `assigneeId`, `projectId` |
| **后端类型定义** | 驼峰命名 | `assigneeId`, `projectId` |
| **API请求/响应** | 驼峰命名 | `assigneeId`, `projectId` |
| **前端类型定义** | 驼峰命名 | `assigneeId`, `projectId` |

### 1.2 枚举值规范

#### 任务优先级
```typescript
type TaskPriority = 'low' | 'medium' | 'high';
```

#### 任务状态
```typescript
type TaskStatus = 'pending' | 'in_progress' | 'completed' | 'blocked' | 'cancelled';
```

#### 项目优先级
```typescript
type ProjectPriority = 'low' | 'medium' | 'high';
```

#### 项目状态
```typescript
type ProjectStatus = 'active' | 'inactive' | 'archived';
```

#### API密钥状态
```typescript
type ApiKeyStatus = 'active' | 'revoked';
```

---

## 二、Task（任务）类型定义

### 2.1 数据库表结构

```sql
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  assignee_id UUID REFERENCES users(id),
  priority VARCHAR(50) DEFAULT 'medium',
  status VARCHAR(50) DEFAULT 'pending',
  status_remark TEXT,
  deliverables JSONB,
  due_date TIMESTAMP,
  parent_id UUID REFERENCES tasks(id) ON DELETE SET NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT tasks_priority_check CHECK (priority IN ('low', 'medium', 'high')),
  CONSTRAINT tasks_status_check CHECK (status IN ('pending', 'in_progress', 'completed', 'blocked', 'cancelled'))
);
```

### 2.2 TypeScript类型定义

```typescript
export interface Task {
  id: string;
  projectId: string;
  name: string;
  description?: string;
  assigneeId?: string;
  parentId?: string | null;
  status: TaskStatus;
  priority: TaskPriority;
  statusRemark?: string;
  dueDate?: Date | null;
  deliverables?: any[] | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateTaskRequest {
  projectId: string;
  name: string;
  description?: string;
  assigneeId?: string;
  parentId?: string | null;
  status?: TaskStatus;
  priority?: TaskPriority;
  dueDate?: Date;
}

export interface UpdateTaskRequest {
  name?: string;
  description?: string;
  assigneeId?: string;
  parentId?: string | null;
  status?: TaskStatus;
  priority?: TaskPriority;
  dueDate?: Date;
}

export interface AcceptTaskRequest {
  assigneeId: string;
}

export interface AcceptanceTaskRequest {
  result: 'approved' | 'rejected';
  comment?: string;
}

export interface BlockTaskRequest {
  blockReason: string;
  relatedTasks?: string[];
}

export interface UnblockTaskRequest {
  comment?: string;
}

export interface TaskStatistics {
  total: number;
  pending: number;
  inProgress: number;
  completed: number;
  blocked: number;
  cancelled: number;
}
```

---

## 三、BlockRecord（阻塞记录）类型定义

### 3.1 数据库表结构

```sql
CREATE TABLE block_records (
  id UUID PRIMARY KEY,
  task_id UUID NOT NULL,
  block_reason TEXT NOT NULL,
  related_tasks UUID[] NOT NULL DEFAULT ARRAY[]::uuid[],
  blocked_by UUID NOT NULL,
  blocked_at TIMESTAMP WITH TIME ZONE NOT NULL,
  resolved_at TIMESTAMP WITH TIME ZONE,
  resolved_by UUID
);
```

### 3.2 TypeScript类型定义

```typescript
export interface BlockRecord {
  id: string;
  taskId: string;
  blockReason: string;
  relatedTasks: string[];
  blockedBy: string;
  blockedAt: Date;
  resolvedAt?: Date | null;
  resolvedBy?: string | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateBlockRecordRequest {
  taskId: string;
  blockReason: string;
  relatedTasks?: string[];
}

export interface ResolveBlockRecordRequest {
  comment?: string;
}
```

---

## 四、Subscription（事件订阅）类型定义

### 4.1 数据库表结构

```sql
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agent_id UUID NOT NULL,
  agent_type VARCHAR(255) NOT NULL,
  target_id UUID,
  event_type VARCHAR(255) NOT NULL,
  callback_url VARCHAR(2048) NOT NULL,
  secret VARCHAR(255),
  is_active BOOLEAN NOT NULL DEFAULT true,
  max_retries INTEGER NOT NULL DEFAULT 5,
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_failed_at TIMESTAMP,
  retry_scheduled_at TIMESTAMP,
  expire_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.2 TypeScript类型定义

```typescript
export interface Subscription {
  id: string;
  agentId: string;
  agentType: string;
  targetId?: string | null;
  eventType: string;
  callbackUrl: string;
  secret?: string | null;
  isActive: boolean;
  maxRetries: number;
  retryCount: number;
  lastFailedAt?: Date | null;
  retryScheduledAt?: Date | null;
  expireAt?: Date | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateSubscriptionRequest {
  agentId: string;
  agentType: string;
  targetId?: string | null;
  eventType: string;
  callbackUrl: string;
  secret?: string;
  maxRetries?: number;
}

export interface UpdateSubscriptionRequest {
  callbackUrl?: string;
  secret?: string;
  isActive?: boolean;
  maxRetries?: number;
}

export interface RetrySubscriptionRequest {
  subscriptionId: string;
}
```

---

## 五、DeadLetterEvent（死信事件）类型定义

### 5.1 数据库表结构

```sql
CREATE TABLE dead_letter_events (
  id UUID PRIMARY KEY,
  subscription_id UUID NOT NULL,
  event_type VARCHAR(255) NOT NULL,
  event_payload JSONB NOT NULL,
  retry_count INTEGER NOT NULL DEFAULT 0,
  last_error TEXT,
  last_failed_at TIMESTAMP WITH TIME ZONE,
  retry_scheduled_at TIMESTAMP WITH TIME ZONE,
  expire_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE NOT NULL,
  updated_at TIMESTAMP WITH TIME ZONE NOT NULL
);
```

### 5.2 TypeScript类型定义

```typescript
export interface DeadLetterEvent {
  id: string;
  subscriptionId: string;
  eventType: string;
  eventPayload: Record<string, any>;
  retryCount: number;
  lastError?: string | null;
  lastFailedAt?: Date | null;
  retryScheduledAt?: Date | null;
  expireAt?: Date | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface RetryDeadLetterRequest {
  eventId: string;
}

export interface BatchRetryDeadLetterRequest {
  eventIds: string[];
}
```

---

## 六、ApiKey（API密钥）类型定义

### 6.1 数据库表结构

```sql
CREATE TABLE api_keys (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agent_id UUID NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
  key_prefix VARCHAR(20) NOT NULL,
  key_hash VARCHAR(255) NOT NULL,
  name VARCHAR(255) NOT NULL,
  status VARCHAR(50) DEFAULT 'active',
  last_used_at TIMESTAMP,
  expires_at TIMESTAMP,
  created_by UUID NOT NULL REFERENCES users(id),
  is_active BOOLEAN NOT NULL DEFAULT true,
  revoked_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT api_keys_status_check CHECK (status IN ('active', 'revoked'))
);
```

### 6.2 TypeScript类型定义

```typescript
export interface ApiKey {
  id: string;
  agentId: string;
  keyPrefix: string;
  keyHash: string;
  name: string;
  status: ApiKeyStatus;
  lastUsedAt?: Date | null;
  expiresAt?: Date | null;
  createdBy: string;
  isActive: boolean;
  revokedAt?: Date | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateApiKeyRequest {
  agentId: string;
  name: string;
  expiresAt?: Date;
}

export interface UpdateApiKeyRequest {
  name?: string;
  status?: ApiKeyStatus;
}

export interface RevokeApiKeyRequest {
  keyId: string;
  reason?: string;
}
```

---

## 七、AuditLog（审计日志）类型定义

### 7.1 数据库表结构

```sql
CREATE TABLE audit_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID REFERENCES projects(id) ON DELETE CASCADE,
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  action VARCHAR(255) NOT NULL,
  resource_type VARCHAR(255) NOT NULL,
  resource_id UUID NOT NULL,
  content JSONB,
  ip_address VARCHAR(50),
  user_agent TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 7.2 TypeScript类型定义

```typescript
export interface AuditLog {
  id: string;
  projectId?: string | null;
  userId?: string | null;
  action: string;
  resourceType: string;
  resourceId: string;
  content?: Record<string, any> | null;
  ipAddress?: string | null;
  userAgent?: string | null;
  createdAt: Date;
}

export interface CreateAuditLogRequest {
  projectId?: string;
  userId?: string;
  action: string;
  resourceType: string;
  resourceId: string;
  content?: Record<string, any>;
  ipAddress?: string;
  userAgent?: string;
}

export interface QueryAuditLogsOptions {
  projectId?: string;
  userId?: string;
  resourceType?: string;
  resourceId?: string;
  startDate?: Date;
  endDate?: Date;
  page?: number;
  pageSize?: number;
}
```

---

## 八、Project（项目）类型定义

### 8.1 数据库表结构

```sql
CREATE TABLE projects (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  manager_id UUID NOT NULL REFERENCES users(id),
  priority VARCHAR(50) DEFAULT 'medium',
  status VARCHAR(50) DEFAULT 'active',
  start_date TIMESTAMP,
  end_date TIMESTAMP,
  due_date TIMESTAMP,
  is_archived BOOLEAN NOT NULL DEFAULT false,
  archive_note TEXT,
  archived_at TIMESTAMP,
  archived_by UUID REFERENCES users(id),
  deleted_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT projects_priority_check CHECK (priority IN ('low', 'medium', 'high')),
  CONSTRAINT projects_status_check CHECK (status IN ('active', 'inactive', 'archived'))
);
```

### 8.2 TypeScript类型定义

```typescript
export interface Project {
  id: string;
  name: string;
  description?: string | null;
  managerId: string;
  priority: ProjectPriority;
  status: ProjectStatus;
  startDate?: Date | null;
  endDate?: Date | null;
  dueDate?: Date | null;
  isArchived: boolean;
  archiveNote?: string | null;
  archivedAt?: Date | null;
  archivedBy?: string | null;
  deletedAt?: Date | null;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateProjectRequest {
  name: string;
  description?: string;
  managerId: string;
  priority?: ProjectPriority;
  status?: ProjectStatus;
  dueDate?: Date | string;
  isArchived?: boolean;
  agentIds?: string[];
}

export interface UpdateProjectRequest {
  name?: string;
  description?: string;
  managerId?: string;
  priority?: ProjectPriority;
  status?: ProjectStatus;
  dueDate?: Date | string;
  isArchived?: boolean;
  agentIds?: string[];
}
```

---

## 九、字段映射表

### 9.1 Task字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | `id` |
| `project_id` | `projectId` | `projectId` | `projectId` |
| `name` | `name` | `name` | `name` |
| `description` | `description` | `description` | `description` |
| `assignee_id` | `assigneeId` | `assigneeId` | `assigneeId` |
| `parent_id` | `parentId` | `parentId` | - |
| `priority` | `priority` | `priority` | `priority` |
| `status` | `status` | `status` | `status` |
| `status_remark` | `statusRemark` | `statusRemark` | - |
| `deliverables` | `deliverables` | `deliverables` | `deliverables` |
| `due_date` | `dueDate` | `dueDate` | `dueDate` |
| `created_at` | `createdAt` | `createdAt` | `createdAt` |
| `updated_at` | `updatedAt` | `updatedAt` | `updatedAt` |

### 9.2 BlockRecord字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | - |
| `task_id` | `taskId` | `taskId` | - |
| `block_reason` | `blockReason` | `blockReason` | - |
| `related_tasks` | `relatedTasks` | `relatedTasks` | - |
| `blocked_by` | `blockedBy` | `blockedBy` | - |
| `blocked_at` | `blockedAt` | `blockedAt` | - |
| `resolved_at` | `resolvedAt` | `resolvedAt` | - |
| `resolved_by` | `resolvedBy` | `resolvedBy` | - |

### 9.3 Subscription字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | - |
| `agent_id` | `agentId` | `agentId` | - |
| `agent_type` | `agentType` | `agentType` | - |
| `target_id` | `targetId` | `targetId` | - |
| `event_type` | `eventType` | `eventType` | - |
| `callback_url` | `callbackUrl` | `callbackUrl` | - |
| `secret` | `secret` | `secret` | - |
| `is_active` | `isActive` | `isActive` | - |
| `max_retries` | `maxRetries` | `maxRetries` | - |
| `retry_count` | `retryCount` | `retryCount` | - |
| `last_failed_at` | `lastFailedAt` | `lastFailedAt` | - |
| `retry_scheduled_at` | `retryScheduledAt` | `retryScheduledAt` | - |
| `expire_at` | `expireAt` | `expireAt` | - |

### 9.4 DeadLetterEvent字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | - |
| `subscription_id` | `subscriptionId` | `subscriptionId` | - |
| `event_type` | `eventType` | `eventType` | - |
| `event_payload` | `eventPayload` | `eventPayload` | - |
| `retry_count` | `retryCount` | `retryCount` | - |
| `last_error` | `lastError` | `lastError` | - |
| `last_failed_at` | `lastFailedAt` | `lastFailedAt` | - |
| `retry_scheduled_at` | `retryScheduledAt` | `retryScheduledAt` | - |
| `expire_at` | `expireAt` | `expireAt` | - |

### 9.5 ApiKey字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | - |
| `agent_id` | `agentId` | `agentId` | - |
| `key_prefix` | `keyPrefix` | `keyPrefix` | - |
| `key_hash` | `keyHash` | `keyHash` | - |
| `name` | `name` | `name` | - |
| `status` | `status` | `status` | - |
| `last_used_at` | `lastUsedAt` | `lastUsedAt` | - |
| `expires_at` | `expiresAt` | `expiresAt` | - |
| `created_by` | `createdBy` | `createdBy` | - |
| `is_active` | `isActive` | `isActive` | - |
| `revoked_at` | `revokedAt` | `revokedAt` | - |

### 9.6 AuditLog字段映射

| 数据库字段 | Sequelize模型 | 后端类型 | 前端类型 |
|-----------|-------------|----------|----------|
| `id` | `id` | `id` | - |
| `project_id` | `projectId` | `projectId` | - |
| `user_id` | `userId` | `userId` | - |
| `action`